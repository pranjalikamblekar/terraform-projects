# terraform-projects

## Project1
```
A basic terraform project to create resources in aws to access apache2 web-server using SSH.
```

## Project2
```
Using VSCode, utilizing several terraform tools and functions to deploy an AWS EC2 instance, along with a VPC, Internet Gateway, Security Groups, etc.
The EC2 instance will serve as a remote development environment that you can log intodirectly from VSCode using functions such as terraform state, console variables, conditionals, template files and more.
Using AWS user-data and a local-exec provisionerto bootstrap the EC2 instance and its connection informationtoyour VS Code SSH config file, allowing you to modifyit for your own projects.
```
